{
  "name": "colors",
  "version": "0.1.0",
  "description": "Colores Css y Scss para tu web",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css": "sass src:dist/css --error-css --style=compressed --update -c --stop-on-error",
    "watch": "sass --watch src:dist/css --error-css --style=compressed --update -c --stop-on-error"
  },
  "keywords": [
    "colors",
    "brand"
  ],
  "author": "Robert Pérez <delfinmundo@gmail.com>",
  "repository":{
    "type": "git",
    "url": "https://github.com/REP98/Colors.git"
  },
  "license": "MIT",
  "devDependencies": {
    "sass": "^1.49.7"
  }
}
